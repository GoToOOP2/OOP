spring:
  datasource:
    url: jdbc:postgresql://prod-server-host:5432/oop_db_prod
    username: prod_user
    password: prod_password
  jpa:
    hibernate:
      ddl-auto: validate # 운영 환경에서는 검증만 수행하거나 'none'으로 설정 권장
    show-sql: false
    properties:
      hibernate:
        format_sql: false


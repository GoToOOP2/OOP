spring:
  datasource:
    url: jdbc:postgresql://dev-server-host:5432/oop_db_dev
    username: dev_user
    password: dev_password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true

cors:
  allowed-origins: "https://dev-frontend.site.com"

spring:
  application:
    name: oop-project
  profiles:
    active: local # 기본값으로 local 실행

server:
  port: 8080

jwt:
  secret: oop-project-jwt-secret-key-must-be-at-least-256-bits-long-here
  expiration: 86400000 # 24시간 (ms)
